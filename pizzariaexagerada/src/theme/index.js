import { createTheme } from '@mui/material/styles';

export const myTheme = createTheme({
  palette: {
    primary: {
      main: '#a860f0',
    },
    secondary: {
      main: '#a860f0',
    },
    white: {
      main: '#fff'
    }
    }
});